image: node:20-alpine

pages:
  stage: deploy
  script:
    - npm install
    - npm run build
    - rm -rf public
    - mkdir public
    # Копируем результат сборки прямо в корень public
    - cp -r out/* public/
  artifacts:
    paths:
      - public
  only:
    - main
