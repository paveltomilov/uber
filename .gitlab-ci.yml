image: node:18

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - npm ci
    - npm run build
  artifacts:
    paths:
      - dist/
  only:
    - main

pages:
  stage: deploy
  script:
    - rm -rf public
    - mkdir -p public/uber # создаем папку uber
    - cp -r dist/* public/uber/ # копируем содержимое dist (НЕ саму папку dist!)
  artifacts:
    paths:
      - public
  only:
    - main
